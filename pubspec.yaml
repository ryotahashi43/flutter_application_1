name: flutter_application_1
description: "A new Flutter project."
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter
  google_sign_in: ^6.2.1
  http: ^0.13.6
  cupertino_icons: ^1.0.8

  # Firebase 関連（バージョンを合わせる）
  firebase_core: ^3.14.0
  firebase_auth: ^5.6.0
  cloud_firestore: ^5.6.0 # これも firebase_core 3.x に対応したバージョン
  intl: 0.19.0
  table_calendar: ^3.0.9
  flutter_localizations:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/c_img.jpg
    - assets/images/geegte_logo.png
